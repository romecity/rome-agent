[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that hit live API endpoints (deselect with '-m "not integration"')
    load: marks load/stress tests (deselect with '-m "not load"')
addopts = -v --tb=short
